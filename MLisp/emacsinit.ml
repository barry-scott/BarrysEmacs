; 
; emacsinit.ml
; 

; 
; Setup EMACS in the windows UI style
; 
; windows - setup the basic windows settings
; windows-extended - add more key binding for windows
; 
(default-emacs-init "windows,windows-extended")

(message "Emacs is initialised.") (sit-for 5)
